---
description: Verify the current implementation plan with native and CLI-backed reviewers
agent: plan
---

Use the `areyousure` skill from `skills/areyousure/SKILL.md` with these arguments:

`$ARGUMENTS`
