---
description: Audit AGENTS.md, CLAUDE.md, and nested agent guidance files for quality, staleness, and drift
agent: plan
---

Use the `agent-docs-audit` skill from `skills/agent-docs-audit/SKILL.md` with these arguments:

`$ARGUMENTS`
